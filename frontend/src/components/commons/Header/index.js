import { StyledHeader } from './style';

import Links from '../Links/index';

const header = (props) => (
    <StyledHeader>
        <p>Insearch</p>
        <Links />
    </StyledHeader>
)

export default header;